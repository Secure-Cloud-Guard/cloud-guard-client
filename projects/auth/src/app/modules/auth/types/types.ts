/**
 * Represents the parameters required for user sign-up.
 * @property {string} email - The email address of the user for sign-up.
 * @property {string} password - The password chosen by the user for sign-up.
 */
export type SignUpParameters = {
  email: string;
  password: string;
};
